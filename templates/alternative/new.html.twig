{% extends 'base.html.twig' %}

{% block title %}New Alternative{% endblock %}

{% block body %}
    <h1 class="card">Create new Alternative</h1>

    <div class="card">
    {{ include('alternative/_form.html.twig') }}
    </div>

    <a class="card" href="{{ path('app_alternative_index') }}">back to list</a>
{% endblock %}
