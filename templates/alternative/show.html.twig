{% extends 'base.html.twig' %}

{% block title %}Alternative{% endblock %}

{% block body %}
    <h1 class="card">Alternative</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ alternative.id }}</td>
            </tr>
            <tr>
                <th>Texte_ambiance</th>
                <td>{{ alternative.texteAmbiance }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ alternative.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a class="card" href="{{ path('app_alternative_index') }}">back to list</a>

    <a class="card" href="{{ path('app_alternative_edit', {'id': alternative.id}) }}">edit</a>

    {{ include('alternative/_delete_form.html.twig') }}
{% endblock %}
