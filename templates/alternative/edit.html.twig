{% extends 'base.html.twig' %}

{% block title %}Edit Alternative{% endblock %}

{% block body %}
    <h1 class="card">Edit Alternative</h1>
    <div class="card">
    {{ include('alternative/_form.html.twig', {'button_label': 'Update'}) }}
    </div>
    <a class="card" href="{{ path('app_alternative_index') }}">back to list</a>
    <div class="card">
    {{ include('alternative/_delete_form.html.twig') }}
    </div>
{% endblock %}
